import { ReactNode } from "react";
export declare type DialogProps = {
    isOpenAll: boolean;
    children: ReactNode;
};
declare const Dialog: ({ isOpenAll, children }: DialogProps) => JSX.Element;
export default Dialog;
