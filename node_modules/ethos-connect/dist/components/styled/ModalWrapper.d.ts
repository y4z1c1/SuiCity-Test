import { ReactNode } from 'react';
export declare type ModalWrapperProps = {
    closeOnClickId: string;
    onClose: () => void;
    isOpenAll: boolean;
    width: number;
    back: (() => void) | null;
    children: ReactNode;
};
declare const ModalWrapper: ({ closeOnClickId, onClose, isOpenAll, width, back, children }: ModalWrapperProps) => JSX.Element;
export default ModalWrapper;
