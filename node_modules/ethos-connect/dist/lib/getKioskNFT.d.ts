import { SuiClient, SuiObjectData, SuiObjectResponse } from "@mysten/sui.js/client";
export declare const isKiosk: (data: SuiObjectData) => boolean;
export declare const getKioskObjects: (client: SuiClient, data: SuiObjectData) => Promise<SuiObjectResponse[]>;
