import { User } from 'types/User';
export declare type loginArgs = {
    email?: string;
    provider?: string;
    apiKey?: string;
};
declare const login: ({ email, provider, apiKey }: loginArgs) => Promise<User | null | undefined>;
export default login;
