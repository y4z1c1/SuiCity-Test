import { getFaucetHost } from '@mysten/sui.js/faucet';
declare type DripSuiParams = {
    address: string;
    networkName: Parameters<typeof getFaucetHost>[0];
};
declare const dripSui: ({ address, networkName }: DripSuiParams) => Promise<{
    transferredGasObjects: {
        amount: number;
        id: string;
        transferTxDigest: string;
    }[];
    error?: string | null | undefined;
}>;
export default dripSui;
